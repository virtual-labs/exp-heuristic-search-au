function randomInt(a, b) {
    return parseInt(Math.random() * (b-a) + a);
}

// document.getElementById("distance-input").addEventListener("change", function(){
//     var input = document.getElementById("distance-input");
//     var distance = input.value;
  
//     graph.edges.add({from: 1, to: 2, distance: distance});
//   });